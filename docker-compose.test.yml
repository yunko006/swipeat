services:
  test-db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: swipeat_test
    ports:
      - "5433:5432"
    tmpfs:
      - /var/lib/postgresql/data
